# Design System

## Theme

克制、可信的企业管理后台。视觉服务于任务，使用熟悉的列表、分区表单、标签页、状态标签和抽屉，不使用装饰性布局替代标准操作。

## Color

- 页面背景：`#f6f8fb`
- 主表面：`#ffffff`
- 次级表面：`#f8fafc`
- 主文字：`#111827`
- 次级文字：`#475569` / `#64748b`
- 边框：`#d8dee8` / `#e5e9f0`
- 主色：`#2563eb`，仅用于主操作、当前选中和关键信息
- 成功、警告、错误使用 Element Plus 语义色，并始终配合文字或图标

## Typography

使用 `Segoe UI`, `PingFang SC`, `Microsoft YaHei`, sans-serif。页面标题 20–24px，分区标题 15–16px，正文与表格 14px，帮助文本 12–13px；不使用展示字体。

## Shape and Elevation

- 小、中、大圆角分别为 6px、8px、10px
- 面板使用 1px 细边框和非常轻的阴影
- 表单控件、按钮、表格和标签统一沿用 Element Plus 与现有管理后台尺寸

## Layout

- 管理页面使用单栏主任务流，页内标签页承载同一模块的连接、工具和记录
- 列表页由模块导航、说明/统计、筛选区、内容区和分页组成
- 新建与编辑使用独立页面的分区表单；窄屏改为单列，操作栏保持可达
- 高级映射默认折叠或置于后续分区，避免和基本信息竞争注意力

## Components

- 使用现有 `AdminListPanel`、`AdminTableToolbar`、`AdminDataTable`、`AdminPagination`
- 空状态解释下一步操作；加载使用骨架；错误明确给出重试入口
- 生命周期统一为草稿、待验证、已发布、异常；连接状态统一为未验证、可用、异常
- 危险操作使用确认对话框，主流程不依赖弹窗完成复杂编辑

## Motion

只在状态切换、抽屉/折叠和反馈中使用 150–250ms 过渡；不使用编排式页面入场动画。
